defaults:
  app_location: "/"  
  api_location: "api"  
  output_location: "build"  

build:
  steps:
    - script: |
        pip install -r requirements.txt
    - script: |
        flask build

- location: "/"
  staticWebApp:
    spa:
      appSource: "build"

