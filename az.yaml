defaults:
  app_location: "src"  
  api_location: "api"  
  output_location: "public"  

build:
  steps:
    - script: |
        pip install -r requirements.txt
    - script: |
        flask build

- location: "/"
  staticWebApp:
    spa:
      appSource: "build"
